# Copy this file to .env and fill in your values.
# BASE_URL must be reachable by the speaker over LAN.
# Use the NAS/server IP and port 8001 (via nginx-ETag proxy).
BASE_URL=http://192.168.1.100:8001

# Speaker IP — needed only for setup-speaker.sh
SPEAKER_IP=192.168.1.200
